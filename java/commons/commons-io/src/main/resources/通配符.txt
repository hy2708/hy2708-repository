"**"通配符
这里说一说“**”这个通配符。
一个星号在 Ant 中表示“文件名中的”任意个字符，所以“*”就表示了所有的文件，
但是“\”不会出现在“文件名中”，所以“*”并不包括子目录下的文件；而两个星号表示 包含“\”在内的任意个字符，
所以“**”匹配诸如“\dira\dirb\file_id.diz”的文件名，从而也就包含任意级子目录中所有的文件。 
进而我们就知道“**/cvs/**”表示在路径中包含“/cvs/”的所有文件，从而我们就可以把cvs 目录下的所有文件排除在压缩包之外。